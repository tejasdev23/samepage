name: SamePage CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Java & Docker
        run: |
          echo "Running Maven Build..."
          # mvn clean package
          echo "Building Docker Image (WIP)..."
          # docker build -t samepage-app .

  gamma:
    needs: build
    runs-on: ubuntu-latest
    environment: Gamma
    steps:
      - name: Deploy to Gamma
        run: echo "Deployment logic coming soon in [SW-3]..."

  onebox:
    needs: gamma
    runs-on: ubuntu-latest
    environment: OneBox
    steps:
      - name: Deploy to OneBox
        run: echo "OneBox canary logic placeholder..."

  prod:
    needs: onebox
    runs-on: ubuntu-latest
    environment: Prod
    steps:
      - name: Full Rollout
        run: echo "Production rollout placeholder..."